#!/usr/bin/env ruby
require_relative '../lib/grouping.rb'
file = open("input/one_card_section.json")
json = file.read
parsed = JSON.parse(json)
trello_summary = Grouping.new(parsed)

puts trello_summary.formatsReference
puts trello_summary.formatsGroup("cards")
